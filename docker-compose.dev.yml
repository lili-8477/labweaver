# Single-user local dev — run the adapter + NATS without nginx, for fast iteration.
# Point the frontend at ws://localhost:4223 directly (no /ws/ prefix proxy).

name: claude-bioflow-dev

services:
  nats:
    image: nats:2.10-alpine
    container_name: claude-bioflow-dev-nats
    command: -c /etc/nats/nats.conf
    ports:
      - "4223:4222"
      - "8081:8080"   # frontend connects here directly: ws://localhost:8081
      - "8223:8222"
    volumes:
      - ./hub/nats.conf:/etc/nats/nats.conf:ro
    networks:
      - bioflow-dev-net

  adapter:
    image: claude-bioflow:dev
    container_name: claude-bioflow-dev-user
    depends_on:
      nats:
        condition: service_started
      postgres:
        condition: service_healthy
    environment:
      ID_HASH: "devuser000"
      NATS_SERVERS: "nats://nats:4222"
      NATS_USER: "agent"
      WORKSPACE_ROOT: "/workspace"
      DEFAULT_PROJECT: "/workspace"
      HOME: "/home/node"
      PG_URL: "postgres://bioflow:${POSTGRES_PASSWORD}@postgres:5432/bioflow"
      USERNAME: "devuser"
      SIDECAR_IMPORT_ON_BOOT: "1"
      # Auto-shutdown kernels idle (no in-flight cell) longer than this.
      # Next execute re-spawns lazily. 0 disables culling.
      KERNEL_IDLE_CULL_MS: "14400000"
      KERNEL_CULL_CHECK_INTERVAL_MS: "60000"
    env_file:
      - path: ./hub/workspaces/devuser/.env
        required: false
    volumes:
      - ./hub/workspaces/devuser/projects:/workspace/projects
      - ./hub/workspaces/devuser/.claude/chats:/workspace/.claude/chats
      - ./hub/workspaces/devuser/.claude/skills:/home/node/.claude/skills
      - ./hub/workspaces/devuser/.claude/agents:/home/node/.claude/agents
      - ./hub/workspaces/devuser/.claude/settings.json:/home/node/.claude/settings.json
    working_dir: /workspace
    networks:
      - bioflow-dev-net

  postgres:
    image: pgvector/pgvector:pg16
    container_name: claude-bioflow-dev-postgres
    env_file: [ ./hub/.env ]
    environment:
      POSTGRES_USER: bioflow
      POSTGRES_DB: bioflow
    volumes:
      - ./hub/postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bioflow"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    networks:
      - bioflow-dev-net

  indexer:
    build: ./hub/indexer
    container_name: claude-bioflow-dev-indexer
    env_file: [ ./hub/.env ]
    environment:
      PG_URL: "postgres://bioflow:${POSTGRES_PASSWORD}@postgres:5432/bioflow"
      WORKSPACES_ROOT: /workspaces
      MAX_CONCURRENT_FILES: "4"
      LOG_LEVEL: debug
    volumes:
      - ./hub/workspaces:/workspaces:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - bioflow-dev-net

networks:
  bioflow-dev-net:
    driver: bridge
